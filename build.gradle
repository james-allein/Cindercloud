allprojects {
    apply plugin: "java"
    apply plugin: 'idea'

    sourceCompatibility = theSourceCompatibility
    targetCompatibility = theSourceCompatibility

    version = theVersion
    group = theGroup

    repositories {
        mavenCentral()
    }
}
